% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundary.R
\name{bl_find_boundary}
\alias{bl_find_boundary}
\title{Find the nearest decision boundary point for each observation}
\usage{
bl_find_boundary(bl_result, data = NULL, tdp = NULL)
}
\arguments{
\item{bl_result}{A \code{"bl_result"} object from \code{bl_assemble()}.}

\item{data}{Data frame to process. Defaults to
\code{bl_result$test_data}. Must contain all columns in \code{var_names}. Can
also be \code{bl_result$train_data} for a global training-data search.}

\item{tdp}{Integer vector of row indices to process. \code{NULL}
(default) processes all rows of \code{data}.}
}
\value{
A list of class \code{"bl_boundary"} with components:
\describe{
\item{\code{B_z}}{n x 2 matrix; Z-space boundary point per observation.}
\item{\code{B_x}}{Data frame (n x p); X-space counterfactual.}
\item{\code{B_pred}}{Numeric vector; model probability at counterfactual
(should be close to \code{cutoff} — validates back-projection quality).}
\item{\code{dist_z}}{Numeric vector; Euclidean distance obs to boundary
in Z-space.}
\item{\code{dist_x}}{Numeric vector; Euclidean distance obs to boundary
in standardised X-space (each dimension divided by \code{X_sd}).}
\item{\code{Z_obs}}{n x 2 matrix; Z-space coordinates of each observation.}
\item{\code{x_obs}}{Data frame; original observations (feature columns).}
\item{\code{pred_obs}}{Numeric vector; model probability at each observation.}
\item{\code{class_obs}}{Integer vector; actual 0/1 labels (\code{NA} if absent).}
\item{\code{z_boundaries_list}}{List of clipped contour segment matrices.}
\item{\code{z_boundary_type}}{Numeric vector; probability level per contour.}
\item{\code{nr_boundaries}}{Integer; number of active contour segments.}
\item{\code{position_store}}{n x 2 integer matrix; contour index used per obs.}
\item{\code{bl_result}}{Reference to the parent \code{bl_result} object.}
}
}
\description{
For each observation in \code{data}, finds the nearest point on the decision
boundary in biplot Z-space, then back-projects it to the original feature
space to produce a counterfactual. The search uses the contour lines
stored in \code{bl_result$biplot_grid$ct}.
}
\section{Counterfactual logic}{

A class-1 observation's counterfactual is the nearest point on a class-0
contour (and vice versa). Selection is based on minimum Euclidean distance
in Z-space, which gives the geometric orthogonal projection from each
observation onto the boundary line.

Boundary segments are first clipped to the training-data convex hull
polygon (in Z-space) and then pruned to retain only points whose
back-projected model prediction is consistent with the contour's
probability level. The training variable ranges (\code{bl_result$train_ranges})
are checked \strong{after} the nearest point is selected, not before. Applying
the range filter as a pre-filter would remove geometrically correct
candidates — particularly for PCA biplots where back-projecting a 2D
Z-space point to p-dimensional X-space sets all non-plotted components to
their mean (rank-2 reconstruction), which can push individual feature
values outside observed ranges even for Z-space points well inside the
convex hull. A message is issued if any selected counterfactual
back-projects outside training ranges; the caller can apply additional
feasibility filtering via \code{bl_result$train_ranges} on the returned \code{B_x}.
}

