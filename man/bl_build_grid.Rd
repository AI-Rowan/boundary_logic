% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biplot_grid.R
\name{bl_build_grid}
\alias{bl_build_grid}
\title{Build the biplot prediction grid}
\usage{
bl_build_grid(
  train_data,
  bl_projection,
  bl_model,
  m = 200L,
  cutoff = 0.5,
  b_margin = NULL,
  rounding = 2L,
  polygon = NULL,
  outlie = 0.9,
  calc_hull = FALSE,
  calc_ct_in_hull = FALSE
)
}
\arguments{
\item{train_data}{Data frame with feature columns (post-filter training
data). Used to project points into Z-space and to define the hull
polygon when \code{polygon} is \code{NULL}.}

\item{bl_projection}{A \code{"bl_projection"} object from
\code{bl_build_projection()}.}

\item{bl_model}{A \code{"bl_model"} object from \code{bl_fit_model()}.}

\item{m}{Integer; grid resolution (m x m points per side).
Default \code{200L}. Increase for finer boundary resolution at the cost of
computation time.}

\item{cutoff}{Numeric; the decision threshold. Default \code{0.5}.}

\item{b_margin}{Numeric; half-width of the band around \code{cutoff} used
to extract contour lines. Default \code{NULL}, which resolves to
\code{1 / (10^rounding)}.}

\item{rounding}{Integer; floor-based rounding precision for grid
scores. Default \code{2}.}

\item{polygon}{An \code{sp::SpatialPolygons} object to use as the hull
boundary. If \code{NULL} (default), a new polygon is computed from
\code{train_data} using \code{aplpack::plothulls()} with \code{fraction = outlie}.}

\item{outlie}{Numeric in (0, 1]; hull fraction used only when
\code{polygon} is \code{NULL}. Default \code{0.9}.}

\item{calc_hull}{Logical; if \code{TRUE}, grid points outside the polygon
are removed from the returned grid. Default \code{FALSE}.}

\item{calc_ct_in_hull}{Logical; if \code{TRUE}, outside-polygon grid cells are
set to \code{cutoff} before contour extraction, so contours are confined to
the hull interior. Default \code{FALSE}.}
}
\value{
A list of class \code{"bl_grid"} with components:
\describe{
\item{\code{Zgrid}}{Numeric matrix (m² x 2); grid coordinates in Z-space.}
\item{\code{Xgrid}}{Data frame (m² x p); grid back-projected to X-space.}
\item{\code{grid_prob}}{Numeric vector (length m²); model scores per grid
point, in \link{0, 1}.}
\item{\code{Z_train}}{Numeric matrix (n x 2); training data projected to
Z-space.}
\item{\code{col_value}}{Character vector (length m²); colour per grid point
on a blue-white-red scale.}
\item{\code{min_val}}{Numeric; lower bound of the square plot region.}
\item{\code{max_val}}{Numeric; upper bound of the square plot region.}
\item{\code{polygon}}{\code{sp::SpatialPolygons} convex hull.}
\item{\code{ct}}{List of contour line objects as returned by
\code{grDevices::contourLines()}.}
\item{\code{xseq}}{Numeric vector (length m); x-axis grid sequence.}
\item{\code{yseq}}{Numeric vector (length m); y-axis grid sequence.}
\item{\code{pred_use}}{Numeric 0/1 vector (length n); predicted class of
each training point.}
\item{\code{pred_col}}{Character vector (length n); colour label per training
point based on confusion category.}
\item{\code{class}}{Numeric 0/1 vector (length n); true class of each
training point.}
}
}
\description{
Creates an m x m grid of points in the 2D biplot Z-space, back-projects
each point to the original feature space, scores every grid point through
the fitted model, and extracts contour lines that approximate the decision
boundary. Also computes the convex hull polygon bounding the training data.
}
\details{
The grid and contours are the foundation for all Phase 2 boundary search
and counterfactual identification operations.
}
\section{Memory efficiency}{

Grid scoring is performed in chunks of 50,000 rows to avoid large peak
memory allocations, matching the behaviour of the original optimised
research script.
}

\section{Contour lines}{

Contours are extracted at \code{cutoff - b_margin} and \code{cutoff + b_margin}.
These two lines bracket the decision boundary. A narrower \code{b_margin}
gives a more precise boundary estimate but may produce fewer contour
points in regions where the model probability changes slowly.
}

\examples{
bl_dat  <- bl_prepare_data(datasets::iris,
                            class_col    = "Species",
                            target_class = "versicolor")
bl_mod  <- bl_fit_model(bl_dat$train_data, bl_dat$var_names,
                         model_type = "GLM")
bl_proj <- bl_build_projection(bl_dat$train_data, bl_dat$var_names,
                                method = "PCA")
bl_grid <- bl_build_grid(bl_dat$train_data, bl_proj, bl_mod, m = 100L)

}
