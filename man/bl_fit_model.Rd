% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{bl_fit_model}
\alias{bl_fit_model}
\title{Fit a classification model for boundary logic analysis}
\usage{
bl_fit_model(
  train_data,
  var_names,
  model_type = "GLM",
  cutoff = 0.5,
  rounding = 2L,
  model_params = list()
)
}
\arguments{
\item{train_data}{Data frame with feature columns named by \code{var_names}
plus a column named \code{"class"} (numeric 0/1).}

\item{var_names}{Character vector of feature column names.}

\item{model_type}{Character scalar specifying the model family. One of:
\code{"GLM"} (logistic regression), \code{"GAM"} (generalised additive model),
\code{"GBM"} (gradient boosting), \code{"LDA"} (linear discriminant analysis),
\code{"SVM"} (support vector machine), \code{"NNET"} (neural network),
\code{"RForrest"} (rpart decision tree), \code{"XGB"} (XGBoost).
Default \code{"GLM"}.}

\item{cutoff}{Numeric decision threshold for computing accuracy.
Default \code{0.5}.}

\item{rounding}{Integer; floor-based rounding precision for predicted
probabilities. Default \code{2}.}

\item{model_params}{Named list of hyperparameter overrides. Unrecognised
keys are silently ignored. Common overrides:
\itemize{
\item GBM: \code{list(n.trees = 1000, shrinkage = 0.05)}
\item NNET: \code{list(size = 10, decay = 0.01)}
\item XGB: \code{list(max_depth = 5, nrounds = 300)}
}}
}
\value{
A list of class \code{"bl_model"} with components:
\describe{
\item{\code{model}}{The fitted model object.}
\item{\code{model_type}}{Character; the model family used.}
\item{\code{var_names}}{Character vector of feature names.}
\item{\code{cutoff}}{The decision threshold.}
\item{\code{rounding}}{The rounding precision.}
\item{\code{accuracy}}{Numeric; classification accuracy on \code{train_data}.}
\item{\code{gini}}{Numeric; Gini coefficient on \code{train_data}.}
}
}
\description{
Fits one of the supported ML classifiers on binary 0/1 training data
and returns the fitted model together with training accuracy and Gini
coefficient.
}
\examples{
bl_dat <- bl_prepare_data(datasets::iris,
                           class_col    = "Species",
                           target_class = "versicolor")
bl_mod <- bl_fit_model(bl_dat$train_data, bl_dat$var_names,
                       model_type = "GLM")
print(bl_mod)

}
