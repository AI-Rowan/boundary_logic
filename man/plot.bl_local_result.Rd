% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_cf.R
\name{plot.bl_local_result}
\alias{plot.bl_local_result}
\title{Plot the local biplot for a bl_local_result}
\usage{
\method{plot}{bl_local_result}(
  x,
  no_grid = FALSE,
  no_points = TRUE,
  no_contour = FALSE,
  cex_z = 0.5,
  label_dir = "Hor",
  tick_label_cex = 0.6,
  ticks_v = 1L,
  which = NULL,
  X_names = NULL,
  label_offset_var = 0L,
  label_offset_dist = 0.5,
  show_arrows = TRUE,
  arrow_col = "grey30",
  contour_col = "black",
  contour_lwd = 1.5,
  contour_lty = 1L,
  print_summary = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{"bl_local_result"} object.}

\item{no_grid}{Logical; if \code{TRUE} the prediction grid is hidden.
Default \code{FALSE}.}

\item{no_points}{Logical; if \code{TRUE} training points are hidden.
Default \code{TRUE} (show only the target and counterfactual). To also
show training data, pass \code{no_points = FALSE}. To overlay test or
holdout data instead, use \code{\link{plot_biplotEZ}} with its
\code{points} argument (which accepts a \code{bl_points} object from
\code{\link{bl_project_points}}).}

\item{no_contour}{Logical; if \code{TRUE} boundary contour lines are
hidden. Default \code{FALSE}.}

\item{cex_z}{Numeric; size of training data points. Default
\code{0.5}.}

\item{label_dir}{Character; biplotEZ axis label direction. \code{"Hor"}
(default) or \code{"Rad"}.}

\item{tick_label_cex}{Numeric; axis tick label size. Default \code{0.6}.}

\item{ticks_v}{Integer; number of ticks per variable axis. Default
\code{1L}.}

\item{which}{Integer vector; indices of variables to draw axes for.
Defaults to all variables.}

\item{X_names}{Character vector; custom variable names for axis
labels. Defaults to \code{bl_result$var_names}.}

\item{label_offset_var}{Integer or integer vector; index/indices of variables
whose axis labels should be shifted outward. \code{0} (default) means no
offset.}

\item{label_offset_dist}{Numeric or numeric vector; outward offset
distance(s) for \code{label_offset_var}. Default \code{0.5}.}

\item{show_arrows}{Logical; if \code{TRUE} (default) an arrow is drawn
from the target to its counterfactual.}

\item{arrow_col}{Character; colour for the CF cross and arrow. Default
\code{"grey30"}.}

\item{contour_col}{Character; colour for contour lines. Default
\code{"black"}.}

\item{contour_lwd}{Numeric; line width for contour lines. Default
\code{1.5}.}

\item{contour_lty}{Integer; line type for contour lines. Default
\code{1L}.}

\item{print_summary}{Logical; print a local CF summary to the console.
Default \code{TRUE}. Set to \code{FALSE} when called from
\code{\link{plot.bl_sparse_result}}.}

\item{...}{Additional arguments (currently ignored).}
}
\description{
Renders the rotated biplot for the best eigenvector pair found by
\code{\link{bl_find_local_cf}} using the same biplotEZ pipeline as
\code{\link{plot_biplotEZ}}. The biplotEZ object is patched with the rotated
loading matrix (\code{Vr_rot}) and rotated training coordinates
(\code{Z_train_rot}) so that axes, grid, points, and contours are all drawn
in the locally-rotated Z-space. Training points are coloured by confusion
type (TP = red, TN = blue, FP = purple, FN = orange). The target observation
is shown as a filled circle coloured by the confusion scheme when its true
class is known from \code{bl_result$test_data}, or red/blue by predicted
class for external points. The nearest boundary point is shown as a cross
with an arrow.
}
