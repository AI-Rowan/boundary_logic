% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_filter.R
\name{bl_filter_outliers}
\alias{bl_filter_outliers}
\title{Remove outliers from training data using a convex hull polygon filter}
\usage{
bl_filter_outliers(bl_data, hull_fraction = 0.9, verbose = TRUE)
}
\arguments{
\item{bl_data}{A \code{"bl_data"} object returned by \code{bl_prepare_data()}
or \code{bl_wrap_data()}.}

\item{hull_fraction}{Numeric in (0, 1]; the \code{fraction} argument for
\code{aplpack::plothulls()}. Values below \code{1} trim the most extreme points.
\code{1.0} retains all points (no trimming). Default \code{0.9}.}

\item{verbose}{Logical; if \code{TRUE}, prints a one-line summary showing
rows retained and removed with percentages. Default \code{TRUE}.}
}
\value{
A list of class \code{"bl_filter_result"} with components:
\describe{
\item{\code{train_data}}{Filtered training data frame.}
\item{\code{test_data}}{Test data frame (filtered to training variable ranges).}
\item{\code{var_names}}{Feature column names.}
\item{\code{num_vars}}{Number of features.}
\item{\code{target_class}}{The positive class value (passed through).}
\item{\code{polygon}}{\code{SpatialPolygons} convex hull in standardised PCA space.}
\item{\code{hull_fraction}}{The fraction value used.}
\item{\code{n_retained}}{Number of training rows retained.}
\item{\code{n_removed}}{Number of training rows removed.}
}
}
\description{
Projects training data into a lightweight 2-PC space, builds a convex hull
enclosing \code{hull_fraction} of the points, removes observations outside the
hull, and reports the number and percentage of rows retained and removed.
}
\details{
This function implements the polygon filter step of Phase 1. Call it
multiple times with different \code{hull_fraction} values to explore the
trade-off between data coverage and outlier removal. Once satisfied, fit
the final model using \code{bl_fit_model()} or \code{bl_wrap_model()} and then
pass the filtered data to \code{bl_build_projection()} and \code{bl_build_grid()}.
}
\section{How the polygon filter works}{

The polygon is constructed in the standardised feature space (all variables
centred and scaled), not in a biplot Z-space, so that it can be applied
before the projection matrix is computed. The convex hull is computed using
\code{aplpack::plothulls()} on the first two principal components of the raw
feature matrix, which is a lightweight proxy for the full biplot projection.
}

\examples{
bl_dat  <- bl_prepare_data(datasets::iris,
                            class_col    = "Species",
                            target_class = "versicolor")
bl_filt <- bl_filter_outliers(bl_dat, hull_fraction = 0.9)
print(bl_filt)

}
