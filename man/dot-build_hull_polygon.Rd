% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hull_utils.R
\name{.build_hull_polygon}
\alias{.build_hull_polygon}
\title{Build a convex hull polygon from projected 2D coordinates}
\usage{
.build_hull_polygon(Z, min_val, max_val, outlie = 0.9)
}
\arguments{
\item{Z}{Numeric matrix with exactly 2 columns (x, y). Typically
the projected training data (\code{Z_train}).}

\item{min_val}{Numeric; lower bound for the square plot region passed to
\code{plothulls()}.}

\item{max_val}{Numeric; upper bound for the square plot region.}

\item{outlie}{Numeric in (0, 1]; the \code{fraction} argument for
\code{aplpack::plothulls()}. Values below 1 trim the most extreme points.
Default \code{0.9}.}
}
\value{
An \code{sp::SpatialPolygons} object representing the convex hull.
}
\description{
Uses \code{aplpack::plothulls()} to compute the hull boundary and wraps
the result as an \code{sp::SpatialPolygons} object. The graphics device
is opened and closed internally; no plot is visible to the user.
}
\keyword{internal}
