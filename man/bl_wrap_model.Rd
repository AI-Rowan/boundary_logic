% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{bl_wrap_model}
\alias{bl_wrap_model}
\title{Wrap an externally fitted model for use in boundary logic analysis}
\usage{
bl_wrap_model(
  model,
  model_type,
  var_names,
  predict_fn = NULL,
  train_data = NULL,
  cutoff = 0.5,
  rounding = 2L
)
}
\arguments{
\item{model}{The fitted model object (or for \code{"XGB"}, the wrapper
list described above).}

\item{model_type}{Character scalar identifying the model family. One of
the standard types or \code{"custom"}.}

\item{var_names}{Character vector of feature column names used during
training.}

\item{predict_fn}{Function with signature \verb{function(model, new_data)}
returning a numeric probability vector. Required when
\code{model_type = "custom"}; ignored otherwise.}

\item{train_data}{Optional data frame (feature columns + \code{"class"}) used
to compute training accuracy and Gini. If \code{NULL}, these are returned as
\code{NA}.}

\item{cutoff}{Numeric decision threshold. Default \code{0.5}.

\strong{Note}: only \code{cutoff = 0.5} gives valid results in the current
methodology. Other values are accepted for exploration but should be
treated with caution.}

\item{rounding}{Integer; floor-based rounding precision. Default \code{2}.}
}
\value{
A list of class \code{"bl_model"} compatible with \code{bl_build_grid()}
and \code{bl_assemble()}.
}
\description{
Use this function when you have fitted your own classification model
outside of \code{bl_fit_model()} — for example, using custom hyperparameters,
a caret workflow, or a model type not natively supported. The wrapped
object is a \code{"bl_model"} compatible with \code{bl_build_grid()} and
\code{bl_assemble()}.
}
\note{
For \code{"XGB"} models, \code{model} must be a list of the form
\verb{list(model = <xgb.Booster>, features = <character vector>)}, matching
the format produced internally by \code{bl_fit_model()}.
}
\section{Supported model types}{

For standard types (\code{"GLM"}, \code{"GAM"}, \code{"GBM"}, \code{"LDA"}, \code{"SVM"},
\code{"NNET"}, \code{"RForrest"}, \code{"XGB"}), the existing prediction dispatch is
used. Your model object must be compatible with the prediction call for
that type (e.g., a \code{glm} object for \code{"GLM"}).

For \code{"custom"} model types, supply a \code{predict_fn} — a function with
signature \verb{function(model, new_data)} that returns a numeric vector of
probabilities (one per row of \code{new_data}).
}

\examples{
# Fit a GLM with custom settings, then wrap it
bl_dat <- bl_prepare_data(datasets::iris, class_col = "Species",
                           target_class = "versicolor")
my_glm <- glm(class ~ ., data = bl_dat$train_data,
              family = binomial(link = "logit"))
bl_mod <- bl_wrap_model(my_glm, model_type = "GLM",
                         var_names  = bl_dat$var_names,
                         train_data = bl_dat$train_data)

# Custom model with predict_fn
my_fn  <- function(m, new_data) predict(m, new_data, type = "response")
bl_mod <- bl_wrap_model(my_glm, model_type = "custom",
                         var_names  = bl_dat$var_names,
                         predict_fn = my_fn,
                         train_data = bl_dat$train_data)

}
