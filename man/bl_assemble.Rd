% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result.R
\name{bl_assemble}
\alias{bl_assemble}
\title{Assemble Phase 1 artifacts into a single result object}
\usage{
bl_assemble(bl_data, bl_filter_result = NULL, bl_model, bl_projection, bl_grid)
}
\arguments{
\item{bl_data}{A \code{"bl_data"} object from \code{bl_prepare_data()}.}

\item{bl_filter_result}{A \code{"bl_filter_result"} from \code{bl_filter_outliers()},
or \code{NULL} if no polygon filtering was applied.}

\item{bl_model}{A \code{"bl_model"} from \code{bl_fit_model()}.}

\item{bl_projection}{A \code{"bl_projection"} from \code{bl_build_projection()}.}

\item{bl_grid}{A \code{"bl_grid"} from \code{bl_build_grid()}.}
}
\value{
A list of class \code{"bl_result"} containing all Phase 1 artifacts.
See \strong{Value} section for the full field list.
}
\description{
Combines the outputs of \code{bl_prepare_data()}, optionally
\code{bl_filter_outliers()}, \code{bl_fit_model()}, \code{bl_build_projection()}, and
\code{bl_build_grid()} into a single \code{bl_result} S3 object. This object is the
reproducible anchor for all Phase 2 operations (counterfactual search,
local interpretation, global interpretation).
}
\section{Phase 1 workflow}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Step 1: prepare data
bl_dat  <- bl_prepare_data(datasets::iris, class_col = "Species",
                            target_class = "versicolor")

# Step 2 (optional): iterative polygon filter
bl_filt <- bl_filter_outliers(bl_dat, hull_fraction = 0.9)

# Step 3: fit final model (on filtered data)
bl_mod  <- bl_fit_model(bl_filt$train_data, bl_filt$var_names,
                         model_type = "GLM")

# Step 4: build projection
bl_proj <- bl_build_projection(bl_filt$train_data, bl_filt$var_names,
                                method = "PCA")

# Step 5: build prediction grid
bl_grid <- bl_build_grid(bl_filt$train_data, bl_proj, bl_mod)

# Step 6: assemble
result  <- bl_assemble(bl_dat, bl_filt, bl_mod, bl_proj, bl_grid)
}\if{html}{\out{</div>}}
}

\section{Value (fields of \code{bl_result})}{

\describe{
\item{\code{train_data}}{Post-filter training data frame.}
\item{\code{test_data}}{Test data frame (filtered to training variable ranges).}
\item{\code{var_names}}{Feature column names.}
\item{\code{num_vars}}{Number of features.}
\item{\code{model}}{Fitted model object.}
\item{\code{model_type}}{Character; model family.}
\item{\code{cutoff}}{Decision threshold.}
\item{\code{rounding}}{Rounding precision.}
\item{\code{V}}{Loading matrix (p x p).}
\item{\code{tV}}{Inverse of V (p x p).}
\item{\code{X_center}}{Column means.}
\item{\code{X_sd}}{Column standard deviations.}
\item{\code{method}}{\code{"PCA"} or \code{"CVA"}.}
\item{\code{standardise}}{Effective standardise flag.}
\item{\code{proj_dims}}{Integer vector of length 2; eigenvector indices used
for the 2D projection plane.}
\item{\code{biplot_obj}}{The biplotEZ object for Phase 2 plotting.}
\item{\code{polygon}}{Convex hull polygon (\code{SpatialPolygons} or \code{NULL}).}
\item{\code{hull_fraction}}{Hull fraction used, or \code{NULL}.}
\item{\code{biplot_grid}}{The full \code{bl_grid} list.}
\item{\code{accuracy}}{Training accuracy.}
\item{\code{gini}}{Training Gini coefficient.}
\item{\code{call}}{The R call that created this object.}
\item{\code{created_at}}{\code{POSIXct} creation timestamp.}
}
}

