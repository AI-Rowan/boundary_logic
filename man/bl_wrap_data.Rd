% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prepare.R
\name{bl_wrap_data}
\alias{bl_wrap_data}
\title{Wrap already-prepared train/test data into a bl_data object}
\usage{
bl_wrap_data(
  train_data,
  test_data = NULL,
  var_names = NULL,
  target_class = NULL
)
}
\arguments{
\item{train_data}{Data frame; feature columns plus a column named
\code{"class"} (numeric 0/1).}

\item{test_data}{Data frame; same column structure as \code{train_data}.
If \code{NULL}, an empty data frame with matching columns is used.}

\item{var_names}{Character vector of feature column names. If \code{NULL}
(default), all columns except \code{"class"} are used.}

\item{target_class}{Optional informational label indicating what the \code{1}
class represents. Does not affect any computation.}
}
\value{
A list of class \code{"bl_data"} compatible with all downstream
functions (\code{bl_filter_outliers()}, \code{bl_build_projection()}, etc.).
}
\description{
Use this function when your data is already split into training and test
sets and the class column is already coded as numeric 0/1. This bypasses
the splitting and multiclass-conversion steps of \code{bl_prepare_data()}.
}
\examples{
\dontrun{
df       <- datasets::iris
df$class <- as.numeric(df$Species == "versicolor")
df$Species <- NULL
train_df <- df[1:100, ]
test_df  <- df[101:150, ]
bl_dat   <- bl_wrap_data(train_df, test_df, target_class = "versicolor")
}

}
