% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projection.R
\name{bl_build_projection}
\alias{bl_build_projection}
\title{Build a PCA or CVA projection matrix for boundary logic analysis}
\usage{
bl_build_projection(
  train_data,
  var_names,
  method = "PCA",
  standardise = TRUE,
  proj_dims = c(1L, 2L),
  bl_model = NULL,
  cva_classes = NULL,
  title = ""
)
}
\arguments{
\item{train_data}{Data frame with feature columns and a \code{"class"} column
(numeric 0/1). The class column is used to derive confusion labels for
CVA when \code{bl_model} is supplied.}

\item{var_names}{Character vector of feature column names.}

\item{method}{\code{"PCA"} or \code{"CVA"}. Default \code{"PCA"}.}

\item{standardise}{Logical; whether to standardise features before PCA.
Ignored (forced to \code{FALSE}) when \code{method = "CVA"}. Default \code{TRUE}.}

\item{proj_dims}{Integer vector of length 2; which eigenvector indices
to use as the biplot plane. Default \code{c(1L, 2L)}.}

\item{bl_model}{A \code{"bl_model"} object from \code{bl_fit_model()} or
\code{bl_wrap_model()}. When \code{method = "CVA"} and \code{cva_classes} is \code{NULL},
\code{bl_model} is used to compute four-class confusion labels (TP, TN, FP,
FN) which are passed as \code{cva_classes}. Ignored for PCA.}

\item{cva_classes}{Factor of class labels, one per row of \code{train_data}.
When supplied, takes precedence over \code{bl_model} for CVA. Use
\code{as.factor(train_data$class)} as a simple alternative when no model
has been fitted.}

\item{title}{Character string for the biplot title. Default \code{""}.}
}
\value{
A list of class \code{"bl_projection"} with components:
\describe{
\item{\code{V}}{Numeric matrix (p x p); loading / eigenvector matrix.}
\item{\code{tV}}{Numeric matrix (p x p); \code{solve(V)}.}
\item{\code{X_center}}{Numeric vector (length p); column means of the
feature matrix.}
\item{\code{X_sd}}{Numeric vector (length p); column standard deviations.}
\item{\code{method}}{\code{"PCA"} or \code{"CVA"}.}
\item{\code{standardise}}{Logical; the effective standardise flag after any
CVA override.}
\item{\code{proj_dims}}{Integer vector; the eigenvector indices used.}
\item{\code{biplot_obj}}{The \code{biplotEZ} S3 object; used for plotting in
Phase 2.}
\item{\code{metric}}{Numeric matrix (p x p); the within-class covariance
\code{W} (or total covariance \code{Sigma} when no class info is supplied) used
as the Mahalanobis metric by \code{bl_find_local_cf()} and
\code{plot.bl_boundary()}.}
\item{\code{metric_inv}}{Numeric matrix (p x p); \code{solve(metric)} computed via
\code{chol2inv(chol(.))} for numerical stability on SPD matrices, with
ridge fallback when singular.}
\item{\code{metric_type}}{Character; one of \code{"W_cva"} (pooled within-class
covariance from CVA class factor), \code{"W_binary"} (pooled within-class
covariance from binary 0/1 class column), or \code{"Sigma"} (total
covariance fallback).}
\item{\code{condition_number}}{Numeric; condition number of the loading matrix
\code{V} (via \code{\link[base:kappa]{base::kappa()}}). Values above 1e10 indicate near-singularity
and trigger a warning at construction time.}
}
}
\description{
Computes the loading matrix V, its inverse tV, and centering/scaling
parameters from the training data. Uses \code{biplotEZ} for the PCA/CVA
computation and biplot object construction.
}
\section{CVA standardisation}{

CVA always forces \code{standardise = FALSE} internally, regardless of the
value supplied by the user. This is because CVA uses within-class
covariance structure that already accounts for scale. The effective
\code{standardise} value is recorded in the returned object.
}

\section{Choosing between PCA and CVA}{

\itemize{
\item \strong{PCA} (\code{method = "PCA"}): suitable for any data; separates
observations by overall variance. Use when class structure is not
well-defined in the feature space.
\item \strong{CVA} (\code{method = "CVA"}): maximises between-class separation
relative to within-class spread. CVA technically requires at least 3
distinct class levels, but biplotEZ supports 2-class data (results may
be degenerate). When \code{bl_model} is supplied, the four confusion
categories (TP, TN, FP, FN) are used as the class labels, satisfying
the 3-class requirement. As a simpler alternative, pass
\code{cva_classes = as.factor(train_data$class)} directly (2 levels only).
}
}

\section{Mahalanobis metric}{

The within-class covariance matrix \code{W} is computed and stored at projection
time so that downstream distance measures can use it without recomputation.
The metric is the same one CVA optimises against -- \code{V} is \code{W}-orthonormal
under exactly this \code{W} -- which keeps distance comparisons internally
consistent. See \verb{2 implementation_summary.txt} Section 4.2.1 for details on
the Cholesky-based inversion and singularity safeguards.
}

\examples{
bl_dat  <- bl_prepare_data(datasets::iris,
                            class_col    = "Species",
                            target_class = "versicolor")
# PCA projection
bl_proj <- bl_build_projection(bl_dat$train_data,
                                bl_dat$var_names,
                                method = "PCA")

# CVA with bl_model -- confusion labels (TP/TN/FP/FN) used automatically
bl_mod <- bl_fit_model(bl_dat$train_data, bl_dat$var_names)
bl_proj_cva <- bl_build_projection(
  bl_dat$train_data,
  bl_dat$var_names,
  method    = "CVA",
  proj_dims = c(1L, 2L),
  bl_model  = bl_mod
)

# CVA alternative: pass binary class factor directly (2 levels only)
bl_proj_cva2 <- bl_build_projection(
  bl_dat$train_data,
  bl_dat$var_names,
  method      = "CVA",
  proj_dims   = c(1L, 2L),
  cva_classes = as.factor(bl_dat$train_data$class)
)

}
