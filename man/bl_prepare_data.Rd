% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prepare.R
\name{bl_prepare_data}
\alias{bl_prepare_data}
\title{Prepare a data frame for boundary logic analysis}
\usage{
bl_prepare_data(
  data,
  class_col,
  target_class = NULL,
  feature_cols = NULL,
  train_fraction = 0.8,
  seed = 121L
)
}
\arguments{
\item{data}{A data frame.}

\item{class_col}{Character scalar; name of the column holding class
labels.}

\item{target_class}{The value in \code{class_col} that should be coded as \code{1}.
All other values become \code{0}. If \code{NULL}, the column must already be
numeric 0/1.}

\item{feature_cols}{Character vector of column names to use as features.
If \code{NULL} (default), all columns except \code{class_col} are used.}

\item{train_fraction}{Numeric in (0, 1); proportion of rows allocated to
training. Default \code{0.8}.}

\item{seed}{Integer; random seed for the train/test split.
Default \code{121}.}
}
\value{
A list of class \code{"bl_data"} with components:
\describe{
\item{\code{train_data}}{Data frame of training rows with columns
\code{var_names} + \code{"class"} (numeric 0/1).}
\item{\code{test_data}}{Data frame of test rows, same structure.}
\item{\code{var_names}}{Character vector of feature column names.}
\item{\code{num_vars}}{Integer; number of features.}
\item{\code{target_class}}{The value that was mapped to \code{1}, or \code{NULL} if
no conversion was applied.}
}
}
\description{
Converts a multiclass or binary class column to a numeric 0/1 indicator,
selects feature columns, and produces a reproducible train/test split.
}
\section{Multiclass to binary conversion}{

If your data has more than two classes (e.g., \code{datasets::iris} has three Species),
supply \code{target_class} to specify which class maps to \code{1}. All other classes
map to \code{0}. For example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# datasets::iris: treat "versicolor" as the positive class
bl_prepare_data(datasets::iris, class_col = "Species", target_class = "versicolor")
}\if{html}{\out{</div>}}

If \code{target_class = NULL}, the class column must already be numeric 0/1.
}

\examples{
# Binary class, versicolor vs rest
bl_dat <- bl_prepare_data(datasets::iris,
                           class_col    = "Species",
                           target_class = "versicolor")
table(bl_dat$train_data$class)

}
