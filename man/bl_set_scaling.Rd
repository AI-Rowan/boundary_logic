% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prepare.R
\name{bl_set_scaling}
\alias{bl_set_scaling}
\title{Record the feature scaling used before model fitting}
\usage{
bl_set_scaling(x, center, scale, method = "z-score")
}
\arguments{
\item{x}{A \code{"bl_data"} object from \code{\link[=bl_wrap_data]{bl_wrap_data()}} or a
\code{"bl_filter_result"} object from \code{\link[=bl_prepare_data]{bl_prepare_data()}} / \code{\link[=bl_filter_outliers]{bl_filter_outliers()}}.}

\item{center}{Numeric vector of per-feature centring constants. Either named
(names must cover \code{x$var_names}; reordered to match) or unnamed and of
length \code{length(x$var_names)} in \code{var_names} order. The attributes produced
by base \code{\link[=scale]{scale()}} can be passed directly, e.g.
\code{center = attr(z, "scaled:center")}.}

\item{scale}{Numeric vector of per-feature scale factors, same length and
naming rules as \code{center}. Must contain no zero or \code{NA} entries.}

\item{method}{Character scalar naming the transform family, recorded for
reference only (default \code{"z-score"}). All features share this single
affine method; per-variable transform families are future work.}
}
\value{
\code{x} with a \code{scaling} component added:
\verb{list(center = <named numeric>, scale = <named numeric>, method = <character>)},
both vectors ordered to match \code{x$var_names}. The object's class is unchanged.
}
\description{
When features are standardised (or otherwise affinely rescaled) \emph{before}
being passed to \code{boundarylogic} -- a common step to improve model fit for
scale-sensitive models -- every biplot axis is labelled in the standardised
units, which is hard to read. \code{bl_set_scaling()} records the per-feature
affine transform \code{standardised = (raw - center) / scale} so that the biplot
plot methods can relabel their axis ticks back into the original (raw) units.
}
\details{
The scaling is \strong{display-only metadata}. It does not change the projection,
the prediction grid, or what the model sees: the model (including a custom
model wrapped via \code{\link[=bl_wrap_model]{bl_wrap_model()}}) continues to receive data in the same
(standardised) units it was trained on. Only the plot methods -- and, where
applicable, the printed counterfactual/target values -- consume the scaling.

Attach it as the final data-preparation step, after \code{\link[=bl_prepare_data]{bl_prepare_data()}} /
\code{\link[=bl_wrap_data]{bl_wrap_data()}} (and after any \code{\link[=bl_filter_outliers]{bl_filter_outliers()}}). The recorded scaling
then flows through \code{\link[=bl_build_result]{bl_build_result()}} / \code{\link[=bl_assemble]{bl_assemble()}} into the \code{bl_result}.
}
\examples{
df       <- datasets::iris
df$class <- as.numeric(df$Species == "versicolor")
df$Species <- NULL
feats    <- setdiff(names(df), "class")
z        <- scale(df[, feats])
df[, feats] <- z
bl_dat   <- bl_wrap_data(df[1:100, ], df[101:150, ])
bl_dat   <- bl_set_scaling(bl_dat,
                           center = attr(z, "scaled:center"),
                           scale  = attr(z, "scaled:scale"))

}
\seealso{
\code{\link[=bl_wrap_data]{bl_wrap_data()}}, \code{\link[=bl_prepare_data]{bl_prepare_data()}}, \code{\link[=bl_build_result]{bl_build_result()}}
}
