% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_points.R
\name{bl_project_points}
\alias{bl_project_points}
\title{Project observations into the biplot Z-space}
\usage{
bl_project_points(
  data,
  bl_result,
  filter_to_polygon = FALSE,
  filter_to_train_ranges = FALSE
)
}
\arguments{
\item{data}{Data frame containing the feature columns named in
\code{bl_result$var_names}. May optionally include a \code{"class"} column
(numeric 0/1) for confusion colouring.}

\item{bl_result}{A \code{"bl_result"} object from \code{bl_assemble()}.}

\item{filter_to_polygon}{Logical; if \code{TRUE}, rows whose projected
coordinates fall outside the training-data convex hull polygon are
removed from the returned object. Default \code{FALSE}.}

\item{filter_to_train_ranges}{Logical; if \code{TRUE}, rows where any feature
value falls outside the min/max range of the training data (as seen by
\code{bl_build_projection()}) are removed before projection. Default \code{FALSE}.}
}
\value{
A list of class \code{"bl_points"} with components:
\describe{
\item{\code{Z}}{Numeric matrix (n x 2); projected coordinates in Z-space.}
\item{\code{pred_prob}}{Numeric vector (length n); model probabilities in
\link{0, 1}.}
\item{\code{pred_class}}{Numeric 0/1 vector (length n); predicted class.}
\item{\code{pred_col}}{Character vector (length n); point colour.}
\item{\code{class}}{Numeric 0/1 vector if \code{"class"} was in \code{data}, else
\code{NULL}.}
\item{\code{inside_polygon}}{Logical vector (length n); \code{TRUE} if within the
convex hull. Always \code{TRUE} when \code{bl_result$polygon} is \code{NULL}.}
}
}
\description{
Projects any data frame — training, test, or new observations — into the
2D biplot plane using the projection matrix V stored in a \code{"bl_result"}
object. Also scores each observation through the fitted model and assigns
point colours:
\itemize{
\item \strong{True labels known} (\code{"class"} column present): confusion colours
TP (red), TN (blue), FP (purple), FN (orange).
\item \strong{Labels unknown} (no \code{"class"} column): red for predicted class 1,
blue for predicted class 0.
}
}
\details{
The projection formula mirrors the one used during Phase 1:
\code{Z = scale(X, center, scale) \%*\% V[, proj_dims]}, where \code{scale} is
\code{X_sd} for PCA with standardisation and \code{FALSE} for CVA.
}
\examples{
bl_dat  <- bl_prepare_data(datasets::iris, class_col = "Species",
                            target_class = "versicolor")
bl_mod  <- bl_fit_model(bl_dat$train_data, bl_dat$var_names)
bl_proj <- bl_build_projection(bl_dat$train_data, bl_dat$var_names)
bl_grid <- bl_build_grid(bl_dat$train_data, bl_proj, bl_mod, m = 50L)
result  <- bl_assemble(bl_dat, bl_model = bl_mod,
                        bl_projection = bl_proj, bl_grid = bl_grid)

# Project training data
train_pts <- bl_project_points(result$train_data, result)

# Project test data (class column present — confusion colours)
test_pts  <- bl_project_points(result$test_data, result)

# Project new observations (no class column — red/blue by prediction)
new_obs   <- result$test_data[1:5, result$var_names]
new_pts   <- bl_project_points(new_obs, result)

}
