% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surrogate.R
\name{bl_surrogate}
\alias{bl_surrogate}
\title{Biplot-region surrogate model}
\usage{
bl_surrogate(bl_result, data = NULL)
}
\arguments{
\item{bl_result}{A \code{"bl_result"} object from \code{bl_assemble()}.}

\item{data}{Data frame to classify. Defaults to
\code{bl_result$train_data}. Must contain all columns in \code{var_names}.}
}
\value{
A list of class \code{"bl_surrogate"} with components:
\describe{
\item{\code{surrogate_pred}}{Integer vector; surrogate class per obs (0 or 1).}
\item{\code{model_pred}}{Integer vector; model-predicted class per obs.}
\item{\code{class_obs}}{Integer vector; true labels (\code{NA} if absent in \code{data}).}
\item{\code{accuracy_vs_model}}{Numeric; agreement rate with model predictions
(computed over in-hull observations only).}
\item{\code{accuracy_vs_labels}}{Numeric; accuracy vs true labels
(\code{NA} if no labels available; in-hull only).}
\item{\code{n_total}}{Integer; total number of observations scored.}
\item{\code{n_in_hull}}{Integer; observations inside the training-data polygon.}
\item{\code{hull_coverage}}{Numeric in \verb{[0, 1]}; fraction of observations inside
the polygon. Accuracy metrics are computed only over in-hull observations.}
\item{\code{boundary_set_nr}}{Integer vector; contour index assigned per obs.}
\item{\code{z_boundary_type}}{Numeric vector; probability level per retained contour.}
\item{\code{Z_obs}}{n x 2 matrix; Z-space coordinates of each observation.}
\item{\code{bl_result}}{Reference to the parent \code{bl_result}.}
}
}
\description{
Assigns each observation's predicted class using the nearest contour line
in Z-space rather than the fitted classifier. The surrogate provides a
purely spatial approximation of the decision boundary confined to the
training-data hull.
}
\section{How the surrogate works}{

The surrogate uses \code{ct_surrogate} contour lines always computed and stored
in \code{bl_result$biplot_grid$ct_surrogate} by \code{bl_build_grid()}. These
contours are generated with cells outside the training-data convex hull
set to \code{cutoff} before extraction, so all contour lines are fully enclosed
within the hull.

Each observation is first projected to Z-space. Observations that fall
\strong{outside} the training-data convex hull polygon are excluded from the
surrogate classification — they receive \code{surrogate_pred = NA} — because
the surrogate regions are defined only within the hull. A message reports
how many observations were excluded. Accuracy is computed on in-hull
observations only.

In-hull observations are assigned to the nearest contour segment in
Z-space. The contour's probability level determines the surrogate class:
level < \code{cutoff} → class 0; level >= \code{cutoff} → class 1.

Accuracy is reported against (in-hull observations only):
\itemize{
\item The fitted model's predictions (\code{accuracy_vs_model})
\item True class labels when present in \code{data} (\code{accuracy_vs_labels})
}
}

