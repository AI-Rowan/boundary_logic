% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pick_point.R
\name{bl_pick_point}
\alias{bl_pick_point}
\title{Interactively identify the nearest data point on an active biplot}
\usage{
bl_pick_point(bl_result, data = NULL)
}
\arguments{
\item{bl_result}{A \code{"bl_result"} object from \code{\link[=bl_assemble]{bl_assemble()}}.}

\item{data}{Data frame to search for the nearest point. Defaults to
\code{bl_result$train_data}. Pass \code{bl_result$test_data} to search test
points, or any other data frame whose features match
\code{bl_result$var_names}.}
}
\value{
Invisibly returns a data frame with one row per click containing:
\describe{
\item{\code{row}}{Row index of the nearest point in \code{data}.}
\item{\code{dist_to_click}}{Z-space Euclidean distance from the click to the
nearest point.}
\item{\code{z1}}{Z-space coordinate 1 of the nearest point.}
\item{\code{z2}}{Z-space coordinate 2 of the nearest point.}
\item{(feature columns)}{All columns named in \code{bl_result$var_names}.}
}
Returns \code{NULL} invisibly if no points were picked.
}
\description{
After rendering a biplot with \code{\link[=plot_biplotEZ]{plot_biplotEZ()}}, call this function and
click anywhere on the plot. For each click the nearest data point is
identified by Z-space Euclidean distance, its row number printed to the
console, and the point highlighted with a yellow circle and row label
on the active plot device. Press \strong{Escape} to stop clicking.
}
\section{Typical usage}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{plot_biplotEZ(bl_results)
picked <- bl_pick_point(bl_results)

# Pick from test data
plot_biplotEZ(bl_results,
              points = bl_project_points(bl_results$test_data, bl_results))
picked <- bl_pick_point(bl_results, data = bl_results$test_data)
}\if{html}{\out{</div>}}
}

