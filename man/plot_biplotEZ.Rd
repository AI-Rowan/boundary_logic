% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_biplot.R
\name{plot_biplotEZ}
\alias{plot_biplotEZ}
\title{Plot the Phase 1 biplot}
\usage{
plot_biplotEZ(
  bl_result,
  points = NULL,
  target_point = NULL,
  target_label = NULL,
  no_grid = FALSE,
  plot_points = TRUE,
  confusion_cols = TRUE,
  no_contour = FALSE,
  new_title = NA,
  cex_z = 0.5,
  label_dir = "Paral",
  label_cex = 1,
  tick_label_cex = 0.6,
  ticks_v = 1L,
  ticks_var = 0L,
  ticks_n = 5L,
  which = NULL,
  X_names = NULL,
  label_offset_var = 0L,
  label_offset_dist = 1.5,
  rotate_deg = 0,
  grid_col = NULL,
  contour_col = "black",
  contour_lwd = 1.5,
  contour_lty = 1L
)
}
\arguments{
\item{bl_result}{A \code{"bl_result"} object from \code{bl_assemble()}.}

\item{points}{A \code{"bl_points"} object from \code{bl_project_points()}.
Controls which observations are plotted as coloured points. If \code{NULL}
(default), training data is projected automatically. Pass
\code{bl_project_points(result$test_data, result)} to show test data instead.}

\item{target_point}{Named numeric vector of feature values (in original
X-space) for an observation to highlight. Names must match
\code{bl_result$var_names}. If \code{NULL} (default), no target is plotted.}

\item{target_label}{Character or integer label displayed inside the
yellow target marker. If \code{NULL} and \code{target_point} is provided, the
point is shown without a label.}

\item{no_grid}{Logical; if \code{TRUE}, the prediction grid is hidden.
Default \code{FALSE}.}

\item{plot_points}{Logical; if \code{TRUE} (default), training data points
are drawn. Set to \code{FALSE} to hide them.}

\item{confusion_cols}{Logical; if \code{TRUE} (default), points are coloured
by confusion category (TP=red, TN=blue, FP=purple, FN=orange) when true
labels are available. Set to \code{FALSE} to colour points by predicted class
only (class 1 = red, class 0 = blue), ignoring true labels.}

\item{no_contour}{Logical; if \code{TRUE}, decision boundary contour lines
are hidden. Default \code{FALSE}.}

\item{new_title}{Character; overrides the biplot title stored in
\code{bl_result}. Use \code{NA} (default) to keep the stored title.}

\item{cex_z}{Numeric; size of training data points (\code{cex}).
Default \code{0.5}.}

\item{label_dir}{Character; biplotEZ axis label direction.
\code{"Paral"} (default) follows the plot border -- labels on the left/right
borders are vertical, top/bottom are horizontal. \code{"Hor"} forces all labels
horizontal. \code{"Orthog"} draws labels perpendicular to the axis direction.}

\item{label_cex}{Numeric; variable name (axis label) size. Default \code{1}.}

\item{tick_label_cex}{Numeric; axis tick label size. Default \code{0.6}.}

\item{ticks_v}{Integer; number of ticks per variable axis.
Default \code{1L}.}

\item{ticks_var}{Integer, integer vector, character, or character
vector; variable(s) whose tick-mark count should override \code{ticks_v}.
Supply variable names (e.g. \code{"Sepal.Length"}) or integer indices
(e.g. \code{c(1L, 3L)}). \code{0} (default) means no override -- every axis uses
\code{ticks_v}.}

\item{ticks_n}{Integer or integer vector; tick-mark count(s) for
the variable(s) named in \code{ticks_var}. A single value applies to all
listed variables; a vector must match the length of \code{ticks_var}.
Default \code{5L}.}

\item{which}{Integer vector; indices of variables to draw axes
for. Defaults to all variables.}

\item{X_names}{Character vector; custom variable names for axis
labels. Defaults to \code{bl_result$var_names}.}

\item{label_offset_var}{Integer, integer vector, character, or character
vector; variable(s) whose axis labels should be shifted outward from the
border. Supply variable names (e.g., \code{"Sepal.Length"}) or integer indices
(e.g., \code{c(1L, 3L)}). \code{0} (default) means no offset.}

\item{label_offset_dist}{Numeric or numeric vector; outward offset distance(s)
in margin lines for \code{label_offset_var}. A single value applies to all
listed variables; a vector must match the length of \code{label_offset_var}.
Useful range 1--3. Default \code{1.5}.}

\item{rotate_deg}{Numeric; angle in degrees to rotate the entire plot
clockwise. Rotates all plotted elements -- grid, training points, contour
lines, variable axes, and the target point -- without rerunning the
pipeline. Default \code{0} (no rotation).}

\item{grid_col}{\code{NULL} (default) or a length-2 character vector
\code{c(class0_col, class1_col)}. When supplied, the prediction grid is
rendered as hard binary colours -- \code{grid_col[1]} for grid points with
predicted probability < 0.5 (class 0) and \code{grid_col[2]} for >= 0.5
(class 1) -- instead of the default blue-white-red gradient. Example:
\code{grid_col = c("blue", "red")}.}

\item{contour_col}{Character; colour for decision boundary contour
lines. Default \code{"black"}.}

\item{contour_lwd}{Numeric; line width for contour lines. Default
\code{1.5}.}

\item{contour_lty}{Integer; line type for contour lines (1 = solid).
Default \code{1L}.}
}
\value{
Invisibly returns \code{bl_result}. Called for its side-effect (plot).
}
\description{
Creates a biplot visualisation of the Phase 1 result using the biplotEZ
rendering pipeline. The plot shows:
\itemize{
\item \strong{Base axes} -- biplotEZ variable axes drawn as a canvas.
\item \strong{Coloured prediction grid} -- each grid point coloured blue
(class 0) through white to red (class 1) by model probability.
\item \strong{Training data points} -- coloured by confusion category.
\item \strong{Variable axes redrawn on top} -- so axes are visible over
the grid and points.
\item \strong{Decision boundary contour lines.}
\item Optionally, a \strong{target observation} highlighted in yellow.
}
}
\section{Point colour legend}{

\describe{
\item{Red}{True positive (actual = 1, predicted = 1).}
\item{Blue}{True negative (actual = 0, predicted = 0).}
\item{Purple}{False positive (actual = 0, predicted = 1).}
\item{Orange}{False negative (actual = 1, predicted = 0).}
\item{Yellow}{Target observation (if supplied).}
}
}

\examples{
bl_dat  <- bl_prepare_data(datasets::iris, class_col = "Species",
                            target_class = "versicolor")
bl_mod  <- bl_fit_model(bl_dat$train_data, bl_dat$var_names)
bl_proj <- bl_build_projection(bl_dat$train_data, bl_dat$var_names)
bl_grid <- bl_build_grid(bl_dat$train_data, bl_proj, bl_mod, m = 100L)
result  <- bl_assemble(bl_dat, bl_model = bl_mod,
                        bl_projection = bl_proj, bl_grid = bl_grid)

plot_biplotEZ(result)

# Highlight a target observation
target <- unlist(result$test_data[1, result$var_names])
plot_biplotEZ(result, target_point = target, target_label = 1)

}
