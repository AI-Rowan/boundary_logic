% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_biplot.R
\name{plot_biplotEZ}
\alias{plot_biplotEZ}
\title{Plot the Phase 1 biplot with decision boundary}
\usage{
plot_biplotEZ(
  bl_result,
  target_point = NULL,
  target_label = NULL,
  no_grid = FALSE,
  no_points = FALSE,
  no_contour = FALSE,
  new_title = NA,
  cex_z = 0.5,
  label_dir = "Hor",
  tick_label_cex = 0.6,
  ticks_v = 1L,
  which = NULL,
  X_names = NULL,
  label_offset_var = 0L,
  label_offset_dist = 0.5,
  contour_col = "black",
  contour_lwd = 1.5,
  contour_lty = 1L
)
}
\arguments{
\item{bl_result}{A \code{"bl_result"} object from \code{bl_assemble()}.}

\item{target_point}{Named numeric vector of feature values (in original
X-space) for an observation to highlight. Names must match
\code{bl_result$var_names}. If \code{NULL} (default), no target is plotted.}

\item{target_label}{Character or integer label displayed inside the
yellow target marker. If \code{NULL} and \code{target_point} is provided, the
point is shown without a label.}

\item{no_grid}{Logical; if \code{TRUE}, the prediction grid is hidden.
Default \code{FALSE}.}

\item{no_points}{Logical; if \code{TRUE}, training data points are hidden.
Default \code{FALSE}.}

\item{no_contour}{Logical; if \code{TRUE}, decision boundary contour lines
are hidden. Default \code{FALSE}.}

\item{new_title}{Character; overrides the biplot title stored in
\code{bl_result}. Use \code{NA} (default) to keep the stored title.}

\item{cex_z}{Numeric; size of training data points (\code{cex}).
Default \code{0.5}.}

\item{label_dir}{Character; biplotEZ axis label direction.
\code{"Hor"} (horizontal, default) or \code{"Rad"} (radial).}

\item{tick_label_cex}{Numeric; axis tick label size. Default \code{0.6}.}

\item{ticks_v}{Integer; number of ticks per variable axis.
Default \code{1L}.}

\item{which}{Integer vector; indices of variables to draw axes
for. Defaults to all variables.}

\item{X_names}{Character vector; custom variable names for axis
labels. Defaults to \code{bl_result$var_names}.}

\item{label_offset_var}{Integer; index of the variable whose axis label
should be shifted outward. \code{0} (default) means no offset.}

\item{label_offset_dist}{Numeric; outward offset distance for
\code{label_offset_var}. Default \code{0.5}.}

\item{contour_col}{Character; colour for decision boundary contour
lines. Default \code{"black"}.}

\item{contour_lwd}{Numeric; line width for contour lines. Default
\code{1.5}.}

\item{contour_lty}{Integer; line type for contour lines (1 = solid).
Default \code{1L}.}
}
\value{
Invisibly returns \code{bl_result}. Called for its side-effect (plot).
}
\description{
Creates a biplot visualisation of the Phase 1 result using the biplotEZ
rendering pipeline. The plot shows:
\itemize{
\item \strong{Base axes} — biplotEZ variable axes drawn as a canvas.
\item \strong{Coloured prediction grid} — each grid point coloured blue
(class 0) through white to red (class 1) by model probability.
\item \strong{Training data points} — coloured by confusion category.
\item \strong{Variable axes redrawn on top} — so axes are visible over
the grid and points.
\item \strong{Decision boundary contour lines.}
\item Optionally, a \strong{target observation} highlighted in yellow.
}
}
\section{Point colour legend}{

\describe{
\item{Red}{True positive (actual = 1, predicted = 1).}
\item{Blue}{True negative (actual = 0, predicted = 0).}
\item{Purple}{False positive (actual = 0, predicted = 1).}
\item{Orange}{False negative (actual = 1, predicted = 0).}
\item{Yellow}{Target observation (if supplied).}
}
}

\examples{
bl_dat  <- bl_prepare_data(datasets::iris, class_col = "Species",
                            target_class = "versicolor")
bl_mod  <- bl_fit_model(bl_dat$train_data, bl_dat$var_names)
bl_proj <- bl_build_projection(bl_dat$train_data, bl_dat$var_names)
bl_grid <- bl_build_grid(bl_dat$train_data, bl_proj, bl_mod, m = 100L)
result  <- bl_assemble(bl_dat, bl_model = bl_mod,
                        bl_projection = bl_proj, bl_grid = bl_grid)

plot_biplotEZ(result)

# Highlight a target observation
target <- unlist(result$test_data[1, result$var_names])
plot_biplotEZ(result, target_point = target, target_label = 1)

}
